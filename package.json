{
  "name": "basic-neural-net",
  "version": "1.0.0",
  "description": "NodeJS (TypeScript) implementation of some very basic machine learning with 2 inputs, 2 hiden neurons and one output neuron",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && node bin/index.js",
    "dev": "node ./node_modules/nodemon/bin/nodemon.js -e ts  --exec \"npm run compile\""
  },
  "author": "Nicholas Mordecai",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12.7.5",
    "nodemon": "^1.19.2",
    "typescript": "^3.6.3"
  }
}
